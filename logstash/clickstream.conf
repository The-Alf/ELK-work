input {
  file {
    path => "/app/weblog.csv"
    start_position => beginning
  }
}

filter {
  csv {
    columns => ["IP", "Time", "URL", "Status"]
    separator => ","
  }
  mutate{
    convert => {
      "Status" => "integer" 
    }
  }
  date {
    match => ["Time", "dd/MMM/yyyy:HH:mm:ss"]
    target => "timestamp"
  }
}

output {
  stdout {
    codec => rubydebug
  }
  elasticsearch {
    hosts => "elasticsearch"
    index => "logs2-csv"
  }
}

